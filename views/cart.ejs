<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>cart</title>
</head>
<body>
<ul>
    <div class="cart page">
        <ul class="list-group">
            <% products.map(e => { %>
            <li class="list-group-item clearfix">
                <div class="col-xs-5">
                    <strong><%= e.Name %>></strong><br/>
                </div>
                <div class="col-xs-2 text-right">
                    <span class="badge"><%= e.quantity %></span>
                </div>
                <div class="col-xs-3 text-right">
                </div>
                <div class="col-xs-2 text-right">
                    <a href="/remove/<%= e.item.ID %>" class="btn btn-default"></i> Remove</a>
                </div>
            </li>
             <% })%>
        </ul>
    </div>
</ul>
</body>
</html>